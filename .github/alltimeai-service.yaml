apiVersion: v1
kind: Service
metadata:
  name: alltimeai-service
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  type: ClusterIP
  selector:
    app: alltimeai
  ports:
  - port: 80
    targetPort: 3000
